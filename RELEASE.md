# PunctuationToggle v1.0.0

PunctuationToggle をリリースしました。

右Commandキーを押すだけで、macOS 標準の日本語入力の句読点を「、。」と「，．」の間で瞬時に切り替えられるメニューバーアプリです。

## 主な機能

- 右Commandを単独で押して離すと句読点を切り替え
- システム設定を開かずにその場で切り替え可能
- 変換中の未確定文字を壊さずに切り替え
- メニューバーに現在のモードを表示
- ログイン時に自動起動を設定可能
- どのアプリでも同じように動作

## 使い方

- 右Command を単独で押して離す: 「、。」 ⇔ 「，．」を切り替え
- 右Command + C などのショートカット: 通常どおり動作
- 左Command: 何もしない

現在のモードはメニューバーの表示で確認できます。

## 対応環境

- macOS 26 以降
- macOS 標準の日本語入力（ローマ字入力）

## インストール

```sh
./install.sh
```

インストール後、表示されるダイアログから「システム設定を開く」を選び、
「プライバシーとセキュリティ」→「アクセシビリティ」で PunctuationToggle を許可してください。

## 注意事項

- 日本語入力の非公開設定キーと通知を利用しているため、今後の macOS 更新で動作に影響が出る可能性があります。
- アプリ終了後も、最後に選択した句読点設定は保持されます。
- 署名が変わる場合があるため、ビルド後は `./install.sh` を使って再インストールすることを推奨します。

## ライセンス

MIT License
